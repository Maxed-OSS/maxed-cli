"""maxed-cli: developer/operator on-ramp for an accounting-tech stack.

This package is pure developer tooling and glue. It scaffolds a local dev
workspace, validates configuration, lints workpaper-spec JSON against a
published JSON Schema, and runs a fully local sandbox connector smoke-test.

It contains no product logic and never connects to real client systems.
"""

__version__ = "0.2.0"
